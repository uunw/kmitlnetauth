<Page x:Class="KmitlNetAuth.Tray.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Dashboard">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24,16,24,24">
        <StackPanel>
            <TextBlock Text="Dashboard" FontSize="28" FontWeight="Bold" Margin="0,0,0,24" />

            <!-- Status Card -->
            <ui:Card Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Connection Status" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Ellipse x:Name="StatusIndicator" Width="12" Height="12"
                            Fill="Gray" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock x:Name="StatusText" Text="Unknown" FontSize="20" FontWeight="SemiBold" />
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <!-- Info Cards -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:Card Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="Username" FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBlock x:Name="UsernameText" Text="-" FontSize="16" FontWeight="Medium" />
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2">
                    <StackPanel>
                        <TextBlock Text="IP Address" FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBlock x:Name="IpAddressText" Text="-" FontSize="16" FontWeight="Medium" />
                    </StackPanel>
                </ui:Card>
            </Grid>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:Card Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="Last Status Change" FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBlock x:Name="LastChangeText" Text="-" FontSize="16" FontWeight="Medium" />
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2">
                    <StackPanel>
                        <TextBlock Text="Uptime" FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBlock x:Name="UptimeText" Text="-" FontSize="16" FontWeight="Medium" />
                    </StackPanel>
                </ui:Card>
            </Grid>

            <!-- Quick Actions -->
            <TextBlock Text="Quick Actions" FontSize="14" FontWeight="SemiBold" Margin="0,8,0,12"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <StackPanel Orientation="Horizontal">
                <ui:Button x:Name="LoginButton" Content="Login Now" Appearance="Primary"
                    Margin="0,0,8,0" Click="OnLoginNowClicked" />
                <ui:Button x:Name="PauseButton" Content="Pause"
                    Click="OnPauseResumeClicked" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
