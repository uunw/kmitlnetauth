<ui:FluentWindow x:Class="KmitlNetAuth.Tray.UpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Update Available"
    Width="400" Height="250"
    MinWidth="400" MinHeight="250"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Update Available" Grid.Row="0" />

        <StackPanel Grid.Row="1" Margin="24,8,24,24" VerticalAlignment="Center">
            <TextBlock x:Name="CurrentVersionText" Margin="0,0,0,4"
                FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="NewVersionText" Margin="0,0,0,16"
                FontSize="15" FontWeight="SemiBold" />

            <ProgressBar x:Name="DownloadProgress"
                Minimum="0" Maximum="100" Value="0"
                Visibility="Collapsed"
                Margin="0,0,0,8" />
            <TextBlock x:Name="ProgressText" Text=""
                Visibility="Collapsed"
                HorizontalAlignment="Center"
                Margin="0,0,0,16"
                FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button x:Name="SkipButton" Content="Skip"
                    Margin="0,0,8,0"
                    Click="OnSkipClicked" />
                <ui:Button x:Name="UpdateButton" Content="Update Now"
                    Appearance="Primary"
                    Click="OnUpdateClicked" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
